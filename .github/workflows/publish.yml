name: Publish to npm

on:
  push:
    tags: ["v*"]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - run: bun install

      - run: bun run gen

      - run: bun run typecheck

      - run: bun test

      - name: Resolve workspace deps for publishing
        run: |
          cd packages/cli
          node -e "
            const pkg = require('./package.json');
            for (const [k,v] of Object.entries(pkg.dependencies || {})) {
              if (v === 'workspace:*') pkg.dependencies[k] = '^' + require('../core/package.json').version;
            }
            require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Publish @vojtaholik/static-kit-core
        working-directory: packages/core
        run: |
          v=$(node -p "require('./package.json').version")
          if npm view "@vojtaholik/static-kit-core@$v" version >/dev/null 2>&1; then
            echo "@vojtaholik/static-kit-core@$v already published, skipping"
            exit 0
          fi
          npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @vojtaholik/static-kit-cli
        working-directory: packages/cli
        run: |
          v=$(node -p "require('./package.json').version")
          if npm view "@vojtaholik/static-kit-cli@$v" version >/dev/null 2>&1; then
            echo "@vojtaholik/static-kit-cli@$v already published, skipping"
            exit 0
          fi
          npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @vojtaholik/create-static-kit
        working-directory: packages/create-static-kit
        run: |
          v=$(node -p "require('./package.json').version")
          if npm view "@vojtaholik/create-static-kit@$v" version >/dev/null 2>&1; then
            echo "@vojtaholik/create-static-kit@$v already published, skipping"
            exit 0
          fi
          npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
